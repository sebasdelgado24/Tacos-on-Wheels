<h1>Tacos on Wheels!</h1>

<h2>Tacos that are ordered!</h2>

<ul>
  {{#each tacos}}
    {{#unless picked_up}}
      {{> tacos/tacos-block picked_up=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>tacos that have been picked up!</h2>

<ul>
  {{#each tacos}}
    {{#if picked_up}}
      {{> tacos/taco-block picked_up=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ta">Taco Name:</label>
    <input type="text" id="ta" name="taco_name">
  </div>

  <div class="form-group">
    <label for="picked_up">picked_up?</label><br>
    <input type="radio" name="picked_up" value="1" checked> picked_up!<br>
    <input type="radio" name="picked_up" value="0"> pending
  </div>

  <button type="submit">Add Taco</button>
</form>
